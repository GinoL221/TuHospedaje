# Traceability Matrix — Close Original User-Story Gaps

**Change**: `close-user-story-gaps` | **Project**: tuhospedaje
**Candidate**: committed WU1–WU11 stacked delivery on `feat/close-user-story-gaps-pr12-wu11`. Commit hashes are intentionally omitted because descendant branches are replayed whenever an earlier stacked boundary changes.
**WU11 additions**: committed Playwright closeout specs, CI fixture hardening, deterministic visual-test networking, regenerated Linux baselines, and this document pair.
**Authority**: the original Digital House Sprint 1–4 PDFs are authoritative for every `US-*` requirement below. The derived `docs/markdown/sprint-{1..4}/*.md` reports are supporting context only, not requirement authority (per spec.md "Purpose" and the orchestrator's explicit instruction).

Disposition values are exactly `compliant`, `partial`, `missing`, or `non-verifiable`, per QA-1. No row is marked `compliant` from code presence or a screenshot alone — every `compliant`/`partial` row cites a real passing automated command and, where the criterion has a user-observable browser behavior, a real browser-evidence ID from `close-user-story-gaps-browser-evidence.md`.

## US #4 + US #8 — Stable random recommendations and pagination

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-4.1 | S1, S2, S3 | Sprint 1–4 PDF, US #4 | `LodgingController`, `LodgingServiceImpl`, `RecommendationPageResponse` (WU1); `Home.jsx` (WU2) | `cd backend && ./mvnw -Dtest=RecommendationServiceContractTest,RecommendationControllerContractTest test` (5/5); `cd frontend && npm test -- Home.test.jsx lodgingService.test.js` (27/27) | BE-REC-01 | N/A | compliant | Bounded page (≤10), no in-page duplicates, non-default order all asserted server-side and reproduced live in BE-REC-01. |
| US-4.2 | S1, S2, S3 | Sprint 1–4 PDF, US #4 | same as US-4.1 | same commands above | BE-REC-01 | N/A | compliant | Forward/back identity (S1) and cross-page uniqueness (S2) verified live against the seeded 38-lodging catalog in BE-REC-01; S3 (independent session randomization) is proven by the deterministic-seed unit tests (two distinct seeds → distinct valid orders). |
| US-4.3 | S1, S2, S3 | Sprint 1–4 PDF, US #4 | `Home.jsx` refresh/reset logic; backend revision hashing | `Home.test.jsx` reset/refresh tests; `RecommendationServiceContractTest` revision-reset case | BE-REC-01 | N/A | compliant | S1 (explicit refresh) reproduced live in BE-REC-01. S2 (session expiry) rests on `sessionStorage`'s native tab-scoped lifetime — a platform guarantee, not app logic; the "first load creates a fresh seed" unit test is the closest direct evidence. S3 (catalog revision) is unit-tested with a controlled revision mismatch fixture (no live catalog mutation was performed against the seeded dev DB during this WU, to avoid disturbing other WU11 fixtures). |
| US-8.1 | S1, S2, S3 | Sprint 1–4 PDF, US #8 | `Home.jsx` pagination controls; `LodgingServiceImpl` page clamping | `Home.test.jsx`; `RecommendationServiceContractTest` (clamp/empty cases) | BE-REC-01 | N/A | compliant | S1 (first/last boundary disabled state) and S3 (failure + retry, no stale content) both reproduced live in BE-REC-01. |

## US #9 — Exact administration route and access

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-9.1 | S1, S2, S3 | Sprint 1–4 PDF, US #9 | `App.jsx` (`isAdminPath`, canonical route, `/admin` alias), `Header.jsx` | `cd frontend && npm test -- App.test.jsx Header.test.jsx Admin.test.jsx` (38/38) | BE-ADMIN-01 | N/A | compliant | S2 (encoded-URL direct load) and S3 (alias redirect) both reproduced live in BE-ADMIN-01 via `AdminBasePage.gotoCanonical()`/`gotoEncoded()`. |
| US-9.2 | S1, S2, S3 | Sprint 1–4 PDF, US #9 | `App.jsx` RBAC wiring (unchanged `RequireAdmin`); `Admin.jsx` mobile guard | `App.test.jsx` (anonymous/non-admin denial); `Admin.test.jsx` (accessible mobile block) | BE-ADMIN-01, BE-ADMIN-02 | N/A | compliant | S3 (mobile/touch unavailable state) is reproduced live at 320×844 with `hasTouch: true` in BE-ADMIN-02 — this is new WU11 evidence; it was previously only unit-tested. |

## US #21 — Representative category images

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-21.1 | S1, S2, S3 | Sprint 1–4 PDF, US #21 | `V6__category_representative_image.sql`, `Category`, `CategoryDTO`, `HttpsImageUrl(Validator)`, `CategoryController`, `CategoryServiceImpl` | `cd backend && ./mvnw -Dtest=CategoryImageIntegrationTest,HttpsImageUrlValidatorTest,CategoryServiceImplTest,CategoryControllerIntegrationTest test` (46/46) | BE-CAT-01 | Cloudinary (image hosting) — see below | compliant | Create-required, valid-edit, and invalid-replacement-non-mutation are all integration-tested against a real (Testcontainers) database; BE-CAT-01 reproduces admin create/edit live against the isolated dev DB. |
| US-21.2 | S1, S2, S3 | Sprint 1–4 PDF, US #21 | `CategoryCard.jsx`, `AdminCategories.jsx` | `cd frontend && npm test -- AdminCategories.test.jsx CategoryCard.test.jsx Home.test.jsx` (53/53) | BE-CAT-01 | Cloudinary — URL format is validated server-side; the backend never fetches or inspects the remote image, so actual image availability/content is a provider boundary, not independently verified | compliant | S3 (feature-icon preservation) is a regression assertion inside `CategoryCard.test.jsx`/`Home.test.jsx`, confirming lodging feature icons are unaffected by the category-image rendering change. |

## US #23 — Safe availability loading and recovery

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-23.1 | S1, S2 | Sprint 1–4 PDF, US #23 | `useAvailability.js`, `ProductDetail.jsx`, `BookingPage.jsx` | `cd frontend && npm test -- useAvailability.test.js ProductDetail.test.jsx BookingPage.test.jsx` (69/69) | BE-AVAIL-01, BE-AVAIL-02 | N/A | compliant | Loading state, occupied-date disabling, and the zero-occupied "ready" usable state are all reproduced live in BE-AVAIL-01/02 with a real authenticated session against the live backend, using route interception only for the availability payload shape (not for auth). |
| US-23.2 | S1, S2, S3, S4, S5 | Sprint 1–4 PDF, US #23 | same as US-23.1 | same commands above | BE-AVAIL-03, BE-AVAIL-04 | N/A | compliant | S1/S2/S3 (initial failure, retry success, repeated failure) and S5 (preflight conflict + backend-conflict-as-final-authority, no partial booking) are all reproduced live in BE-AVAIL-03/04. S4 (stale-after-refresh-failure) is unit-tested in `useAvailability.test.js`; it was not separately reproduced live in this pass because forcing a `ready → stale` transition needs a two-step route sequence identical in shape to the already-covered S3 repeated-failure case. |

## US #28 — Completed-stay rating eligibility

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-28.1 | S1–S7 | Sprint 1–4 PDF, US #28 | `ReservationRepository`, `RatingServiceImpl`, `RatingController` | `cd backend && ./mvnw -Dtest=RatingEligibilityServiceTest,RatingEligibilityControllerIntegrationTest,RatingServiceImplTest,RatingControllerIntegrationTest test` (19/19) | BE-REV-01, BE-REV-03 | N/A | compliant | S1 (eligible), S2 (checkout-boundary-day), S4 (cancelled) are integration-tested against a real database. S6 (wrong lodging/user) is proven twice: at the unit level (Mockito) AND now live end-to-end in BE-REV-01/BE-REV-02, where the same fixture user is genuinely ineligible on a lodging they never reserved. S5 (multiple reservations, at least one eligible) is proven only at the unit level (`Clock.fixed` Mockito test), not with dedicated real-database multi-row proof — an explicitly flagged, budget-driven WU8 deferral; still real automated evidence, not absent. |
| US-28.2 | S1, S2, S3, S4 | Sprint 1–4 PDF, US #28 | `ReviewsSection.jsx`, `useRatingEligibility.js` | `cd frontend && npm test -- ReviewsSection.test.jsx ProductDetail.test.jsx primaryActionContrast.test.js` (50/50) | BE-REV-01, BE-REV-02, BE-REV-03 | N/A | compliant | S1 (anonymous, no form), S2 (ineligible), S3 (eligibility-fetch failure + retry), and S4 (server rejects submission, score/comment retained) are all reproduced live in BE-REV-01..03 against the real backend and a directly-seeded past-checkout `CONFIRMED` reservation fixture (see browser-evidence doc for the exact fixture). |

## US #34 — WhatsApp handoff control

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| US-34.1 | S1, S2, S3 | Sprint 1–4 PDF, US #34 | `WhatsAppButton.jsx`, `WhatsAppButton.css` | `cd frontend && npm test -- WhatsAppButton.test.jsx Footer.test.jsx` (16/16) | BE-WA-01, BE-WA-02, BE-WA-03 | N/A | compliant | S3 (responsive placement, no overflow) is reproduced live at desktop, 320×844, and 390×844 in BE-WA-02/BE-WA-03/BE-WA-04 (mobile-shell). |
| US-34.2 | S1, S2 | Sprint 1–4 PDF, US #34 | same as US-34.1 | same command above | BE-WA-01 | WhatsApp (wa.me) — handoff-only, see below | compliant | S1 (valid handoff, non-delivery-claiming copy) is reproduced live in BE-WA-01 with a stubbed `window.open`. S2 (invalid/missing number) cannot be exercised in this live e2e pass because `VITE_WHATSAPP_NUMBER` is inlined at Vite **build** time, not readable/overridable at browser runtime — it is fully covered instead by `WhatsAppButton.test.jsx`'s `vi.stubEnv` unit tests (5 invalid/missing variants), which is real automated evidence at a different test layer, not a gap. |
| US-34.3 | S1, S2, S3 | Sprint 1–4 PDF, US #34 | same as US-34.1 | same command above | BE-WA-05, BE-WA-06 | WhatsApp (wa.me) | partial | S1 (popup blocked) and S2 (handoff unavailable/assignment exception) are compliant, reproduced live in BE-WA-05/BE-WA-06 with a fake `window.open` double (real WhatsApp is never contacted). S3 is **structurally non-verifiable**: TuHospedaje has no WhatsApp provider receipt, callback, or delivery/read evidence by design (spec's own explicit boundary) — this is expected, not a defect, and the requirement row is `partial` only because one of its three scenarios is definitionally unobservable from this system. |

## Evidence, regression, and quality gates

| Criterion | Scenarios | Authority | Changed paths/symbols | Automated command/test | Browser evidence | Provider boundary | Disposition | Notes |
|---|---|---|---|---|---|---|---|---|
| QA-1 | S1 | Sprint 1–4 PDF, QA section | This document pair | N/A (documentation criterion) | N/A | N/A | compliant | Every `US-*` requirement above has authority, changed paths, an automated command with a real result, and (where user-observable) a browser-evidence ID. US #19 is labeled optional below. |
| QA-2 | S1, S2, S3 | Sprint 1–4 PDF, QA section | All WU1–WU11 changed paths | `cd backend && ./mvnw verify` (493/493, JaCoCo met); `cd frontend && npm run coverage && npm run lint && npm run build` (493/493, 0 lint problems, build succeeds); targeted Reviews + WhatsApp browser verification (20/20); final published PR #94 GitHub Actions workflow (all browser projects green) | N/A | N/A | compliant | S2 (cumulative regression) is covered by the full backend/frontend suites and the final published CI workflow. S3 (deterministic verification) holds: backend uses `Clock.fixed`/injected business clock, frontend uses controlled seeds/mocks, e2e uses route interception for non-deterministic/provider boundaries, and visual tests fulfill non-local images locally. Linux visual baselines were regenerated through the authorized workflow and inspected before acceptance. The replayed stack must rerun these checks after publication. |
| QA-3 | S1, S2, S3 | Sprint 1–4 PDF, QA section | e2e specs listed in the browser-evidence doc | `cd e2e && npx playwright test --project=chromium`, `--project=firefox`, `--project=mobile-chromium` | All `BE-*` IDs in the browser-evidence doc | Cloudinary, WhatsApp, SMTP — each explicitly stubbed/local, see below | compliant | S1 (desktop Chromium+Firefox) and S2 (320×844 + 390×844 mobile Chromium) are both satisfied — every changed flow in this change has at least one desktop and, where a responsive requirement applies (admin mobile guard, WhatsApp placement), one mobile evidence row. S3 (provider boundaries) — Cloudinary: URL-format-only validation, no fetch; WhatsApp: local handoff only via a fake `window.open`, real `wa.me` never contacted; SMTP: `MAIL_SMTP_ENABLED=false`, mail is console-logged only and was not independently re-verified in this WU11 pass (already proven by the pre-existing `feat/email-outbox-transport` unit/integration suite, out of this change's scope). |
| US #19 | — | Sprint 1–4 PDF, US #19 | Not touched by this change | N/A | N/A | Email delivery (registration confirmation) | non-verifiable | Explicitly optional and outside the evaluated critical path per spec.md's "Out of scope" section. No implementation or evidence was produced for it in `close-user-story-gaps`. |

## Final verification results referenced above

- **Backend** (`cd backend && ./mvnw verify`): 493/493 tests, 0 failures/errors, JaCoCo coverage checks met, `BUILD SUCCESS`.
- **Frontend** (`cd frontend && npm run coverage`): 493/493 tests passing with coverage thresholds met. `npm run lint`: 0 problems. `npm run build`: succeeds. The previously ambiguous ProductDetail availability-status assertion is now scoped to its exact accessible text and role.
- **Targeted browser fixtures**: Reviews + WhatsApp passed 20/20 across Chromium and Firefox after creating the documented completed-reservation fixture and setting `VITE_WHATSAPP_NUMBER` before the Vite build.
- **Published PR #94 CI**: backend, frontend, Chromium, Firefox, and mobile Chromium jobs were green after Linux baseline regeneration and deterministic external-image handling. This records the published candidate before the local history-only PR #86/#87 boundary replay; the rewritten candidate requires a fresh CI run after publication.
