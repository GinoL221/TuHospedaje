# Home retry control touch targets

- [ ] Issue #303: add a failing browser regression, style Home search/category retry controls for visible focus and >=44×44 CSS-pixel targets, verify desktop and 390×844/320×844 mobile behavior, and record one signed work-unit commit.

Branch `fix/home-retry-control-targets` starts from `origin/main` `624bbb3e2d97385c43176339c6e9adfbfe3963bd` in an isolated new worktree. Preserve the previous Home retry worktrees and unrelated work. No `.env*`, dependency install, backend/Docker, push, PR, merge or cleanup.

Evidence: approved issue #303. No RED observed (configured TDD mode not active). Isolated Chromium regression passed 3/3 at desktop/390/320 with mocked APIs, measured >=44×44, focus, no overflow and independent retry-to-empty recovery. Focused Home/search 44/44; full frontend 641/641 across 66 files with coverage thresholds passed; ESLint, frontend format check, Vite build and diff check passed. Existing `act` and unresolved `esbuild` warnings remain. Signed commit and native review pending; CI/provider/assistive-technology checks not claimed.
