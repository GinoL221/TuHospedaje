# Home retry control touch targets

- [x] Issue #303: style Home search/category retry controls for visible focus and >=44×44 CSS-pixel targets, verify desktop and 390×844/320×844 mobile behavior, and record one signed work-unit commit. A pre-change RED run was not observed.
- [ ] PR #304 review correction: use existing design tokens to make light-theme alert/button normal/hover text contrast >=4.5:1; add computed browser checks, verify without `.env*`, and make one signed local commit. No push/merge authorization.

Branch `fix/home-retry-control-targets` starts from `origin/main` `624bbb3e2d97385c43176339c6e9adfbfe3963bd` in an isolated new worktree. Preserve the previous Home retry worktrees and unrelated work. No `.env*`, dependency install, backend/Docker, push, PR, merge or cleanup.

Evidence: approved issue #303. No RED observed (configured TDD mode not active). Isolated Chromium regression passed 3/3 at desktop/390/320 with mocked APIs, measured >=44×44, focus, no overflow and independent retry-to-empty recovery. Focused Home/search 44/44; full frontend 641/641 across 66 files with coverage thresholds passed; ESLint, frontend format check, Vite build and diff check passed. Existing `act` and unresolved `esbuild` warnings remain. Signed work-unit commit `ad64b9ce7326a7701521228fa981000f7df19e53` has a valid GPG signature. Native review `review-a9dfe5414aa2a823` approved and acknowledged that candidate. Native assess was unavailable (`schema-incompatible`); independent verification was performed. PR #304 is open with `Closes #303`, `type:bug`, and 6/6 passing CI jobs for the published head `592bc8b`. Independent review then identified approx. 2.6:1 light-theme text contrast in the new alert/buttons. Local correction implemented but not yet committed or pushed: computed contrast assertions RED failed 3/3 at 2.835:1, scoped CSS changes made the isolated browser GREEN 3/3 at desktop/390/320 for alert/button normal and hover >=4.5:1 plus existing focus/size/overflow/recovery. Independent browser 3/3, frontend 641/641 with coverage thresholds, ESLint, format, build, syntax and diff checks passed; known `act`/`esbuild` warnings remain. A signed work-unit commit and native review are pending; the earlier review remains bound to `ad64b9c`. Provider/assistive-technology checks and further push/merge are not claimed or authorized.
